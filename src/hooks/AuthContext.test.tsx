import React from "react";

describe("Login", () => {
  test("Displays user info on success", () => {});

  test("Displays error on failure", () => {});
});

describe("Logout", () => {
  test("Clears user data", () => {});
});

describe("Verify auth state", () => {
  test("Displays message from backend if authorized", () => {});

  test("Displays error message if unauthorized", () => {});
});
